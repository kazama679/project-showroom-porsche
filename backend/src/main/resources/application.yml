spring:
  messages:
    basename: text/messages
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  application:
    name: base_spring_boot
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/showroom_porsche?createDatabaseIfNotExist=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:123456}
  jooq:
    sql-dialect: org.hibernate.dialect.MySQL8Dialect
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  # Redis configuration
  data:
    redis:
      host: localhost
      port: 6379

  # Mail configuration (Gmail SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: quanglienha123@gmail.com
    password: trsjvnhyuhhoxnzl
    properties:
      mail:
        smtp:
          auth: true
          starttls:   
            enable: true

jwt:
  secret:
    key: Ascmkkqdnmsdfrtgsyuhidj21ue120938219321ndsad
  expired:
    access: 2592000000

